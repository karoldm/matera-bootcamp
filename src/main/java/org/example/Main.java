package org.example;

import org.example.classes.abstractClasses.Account;
import org.example.classes.abstractClasses.CheckingAccount;
import org.example.classes.abstractClasses.SavingAccount;
import org.example.classes.anonymousClasses.AccountsWallet;

public class Main {
    public static void main(String[] args) {

    }
}